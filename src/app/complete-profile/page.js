import CompleteProfile from "../components/complete-profile";

export default async function PlayerProfile() {
  return (
  <main className="w-[95%] m-auto flex flex-col">
    <h1>Just a few more things before we get you on your way</h1>
      <CompleteProfile />;
  </main>
  )
}
